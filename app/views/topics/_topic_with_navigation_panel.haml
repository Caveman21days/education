- content_for :left_panel do
  .border-right
    .nav.flex-column.nav-pills#v-pills-tab{ role: "tablist", "aria-orientation" => "vertical" }
      / %a.nav-link.active.text-dark#v-pills-chavo-tab{ "data-toggle" => "pill", href: "#v-pills-chavo", role: "tab", "aria-controls" => "v-pills-chavo", "aria-selected" => "true" } ЧаВо

      - Topic.topic_sections.each do |t|
        %a.nav-link.text-dark{ id: "v-pills-#{t[0]}-tab", "data-toggle" => "pill", href: "#v-pills-#{t[0]}", role: "tab", "aria-controls" => "v-pills-#{t[0]}", "aria-selected" => "false" }= t[1]

      %br
      %a.nav-link.text-dark.btn.btn-secondary{  href: new_topic_path, role: "tab", "aria-selected" => "false", type: "button" } Новая тема


- content_for :center do
  .tab-content#v-pills-tabContent
    / .tab-pane.fade.show.active#v-pills-chavo{ role: "tabpanel", "aria-labelledby" => "v-pills-chavo-tab" }
    /   .col-12.card
    /     .row
    /       .col-12.card-header
    /         %h5.text-left ЧаВо
    /       .col-12.card-body
    /         %p ЧаВо


    - Topic.topic_sections.each do |section|
      .tab-pane.fade{ id: "v-pills-#{section[0]}", role: "tabpanel", "aria-labelledby" => "v-pills-#{section[0]}-tab" }
        .col-12.card
          .row
            .col-12.card-header
              .row
                .col-12
                  %h5.text-left= section[1]

            .col-12.card-body
              %table.table
                %thead.table-secondary
                  %tr
                    %th{ scope: "col" } Тема
                    %th{ scope: "col" } Автор
                    %th{ scope: "col" } Дата создания
                    %th{ scope: "col" }
                %tbody
                  - Topic.where(section: section[1]).each do |t|
                    %tr
                      %td= t.title
                      %td= "#{t.author.l_name} #{t.author.f_name}"
                      %td= t.created_at.strftime("%d.%m.%Y %H:%M")
                      %td
                        = link_to 'Перейти', topic_path(t.id), class: 'btn btn-outline-secondary btn-sm'
