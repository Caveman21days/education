.actions
  = link_to 'Добавить пользователя', situational_user_assigments_path


.page-content
  .fancy-table-container.issues-table
    %table
      %thead
        %tr
          %th.name Пользователь
          %th.role Роль
      %tbody
        - issuable.users.each do |user|
          %tr
            %th
              - if can? :read, user
                = link_to user.full_name, user_path(user)
              - else
                = user.full_name
            %td
              = user.role_in(issuable).name
